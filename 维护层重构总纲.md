# RimTalk Expand Memory - 维护层重构总纲

> 文档版本：v2.2
> 状态：重构已结束
> 基线：当前仓库实际实现

## 1. 文档定位

本文是维护层（含 AI 层）重构的归档总纲，用于记录原重构目标、最终落地结构和实际执行偏差。

配套文档：

- `维护层.md`：当前维护层实际行为。
- `AI层重写方案.md`：当前 AI 层实际行为。
- `MemoryMaintainer重构执行方案.md`：Maintainer 抽取过程记录。

重构已经结束。本文不再提出后续阶段；原方案中未按初始设想落地的条目，按最终采用的实现更新后标记完成。

## 2. 重构结果

重构前，个人记忆维护、总结和 AI 调用分散在 `MemoryManager`、`FourLayerMemoryComp`、UI、`AIRequestManager` 与 `IndependentAISummarizer` 中。

重构后形成以下实际结构：

```text
FourLayerMemoryComp
  - 持有四层列表、Pawn 私有 SummarizedIds 和 Scribe 状态
  - CompTickRare 驱动 Pawn 级维护
  - 持有 MemoryMaintainer / MemorySummarizer
          |
          +-> MemoryMaintainer
          |     衰减、ABM 到期、清理、容量、Pin、删除
          |
          +-> MemorySummarizer
                自动/手动总结与归档、提示词、结果回写
                         |
                         v
                    AIService
                    GameComponent 队列、10 秒启动间隔
                         |
                         v
                    ClientPool
                    多配置 fallback
                         |
                         v
                    OpenAIClient
                    UnityWebRequest + OpenAI 兼容协议

MemoryManager
  - 冷启动延迟
  - Pawn 状态常识更新
  - CommonKnowledge / PromptCache
```

`IndependentAISummarizer` 和 `AIRequestManager` 已删除。`MemoryManager` 不再负责个人记忆的衰减、总结和归档调度。

## 3. 最终职责

### 3.1 FourLayerMemoryComp

- [x] 持有 ABM、SCM、ELS、CLPA 四层集合。
- [x] 持有并持久化 Pawn 私有的 `SummarizedIds`，以来源 `OriginId` 隔离各 Pawn 的处理状态。
- [x] 负责四层集合的 Scribe 读写和读档后 null 修复。
- [x] 持有 `JobMemoryCapturer`、`MemoryMaintainer`、`MemorySummarizer`。
- [x] 通过 `CompTickRare()` 控制每小时和每日维护粒度。
- [x] 将机械维护和语义总结委托给对应协作对象。
- [x] 保留现有 public `List<MemoryEntry>` 属性，兼容当前捕获、UI、导入和检索调用。

实际边界：Comp 仍提供可变列表；本轮重构以拆分职责和统一执行位置为目标，没有继续改造为只读集合或统一写入门面。

### 3.2 MemoryMaintainer

- [x] 抽取 SCM、ELS、CLPA 的 Activity 衰减。
- [x] 抽取 ABM 寿命到期处理。
- [x] 抽取低活跃清理。
- [x] 抽取 SCM、ELS、CLPA 容量治理。
- [x] 抽取 Pin 状态和 ABM 到 SCM 的直接迁移。
- [x] 统一 UI 删除入口为 `Remove()`。
- [x] 保持为绑定单个 Comp 的无持久状态 POCO。

实际边界：`MemoryMaintainer` 是机械维护实现位置，但不是所有列表写入的唯一入口；捕获、创建、导入及总结结果仍按当前设计直接写入对应列表。

### 3.3 MemorySummarizer

- [x] 统一每日自动总结和 UI 主动总结。
- [x] 统一周期自动归档和选中归档。
- [x] 将源条目选择、按天分组和提示词构建从旧模块集中到 Summarizer。
- [x] 总结和归档产物统一使用 `MemoryType.Summarization`。
- [x] 合并源条目的 Importance、GameTick、tags 和 keywords。
- [x] 使用非持久 `SummarizingMemories` 引用集合防止同一对象被重复提交。
- [x] AI 成功后创建 ELS/CLPA，并把源条目的 `OriginId` 写入当前 Pawn 的 `SummarizedIds`。
- [x] AI 失败时不创建目标，dispose 从 `SummarizingMemories` 移除源对象。
- [x] 总结/归档后保留源条目，由寿命、衰减和容量规则自然处置。

实际边界：结果回写使用闭包中的 `MemoryEntry` 引用并直接加入目标列表；本轮没有引入 request ID、条目版本或独立 ApplyResult 模型。在途状态也按对象引用而非 `OriginId` 跟踪，因此 `RoundMemory` 在请求期间私有化后，新副本理论上可从其他入口重复提交；该行为作为已知限制保留。

### 3.4 MemoryManager

- [x] 删除个人记忆的全局衰减入口。
- [x] 删除每日总结和周期归档的世界级调度。
- [x] 删除 Pawn 级摘要创建和列表操作。
- [x] 保留 200 ticks 冷启动缓冲。
- [x] 保留每 2500 ticks 的 Pawn 状态常识更新和记录清理。
- [x] 保留 `CommonKnowledgeLibrary` 与 `PromptCache` 的所有权和存档。

最终选择：原案中的“世界级维护任务编排器”没有保留。总结和归档改由每个 Pawn 的 `CompTickRare()` 哈希分散触发，跨 Pawn 的 AI 请求启动节奏由 `AIService` 统一控制。

### 3.5 AI 层

- [x] 删除 `IndependentAISummarizer`。
- [x] 删除 `AIRequestManager`。
- [x] 新建 `AIService : GameComponent` 统一请求队列和回调派发。
- [x] 使用现实时间 10 秒作为请求启动间隔。
- [x] 新建 `IAIClient`、`ClientPool` 和 `AIClientFactory`。
- [x] 支持按配置顺序执行客户端 fallback。
- [x] 跟随 RimTalk `CloudConfigs` 或使用本模组 `ApiConfigs`。
- [x] 所有 Provider 统一使用 OpenAI 兼容 chat/completions 协议。
- [x] 使用 `UnityWebRequest` 实现连接/读取超时和有限重试。
- [x] 使用 `Payload.IsValid` 作为池级成功判定。
- [x] 配置变化后重置 ClientPool。
- [x] 配置验证限制在存档内执行。
- [x] 删除旧独立 API 字段、Player2、Gemini 原生协议和 Prompt Caching 专有逻辑。

最终选择：AI 层返回 `Payload`，业务 callback 仍为 `Action<string>`；没有增加持久任务、请求去重、取消令牌或结果缓存。

### 3.6 UI、捕获和导入

- [x] UI 总结调用 `MemorySummarizer.ManualSummarize()`。
- [x] UI 归档调用 `MemorySummarizer.Archive()`。
- [x] UI 删除调用 `MemoryMaintainer.Remove()`。
- [x] 一键总结殖民者调用各 Pawn 的 `AutoSummarize()`。
- [x] 捕获层继续按现有接口写入 ABM。
- [x] 创建和导入继续按目标层写入列表。

最终选择：UI 不再复制总结/归档算法，但捕获、创建和导入的直接列表写入作为现有接口保留。

已知限制：当前记忆导出格式只包含四层 `MemoryEntry`，不包含 Comp 持有的 `SummarizedIds`；导入后已处理条目可能再次总结或归档。导入导出状态和格式版本将在未来单独完善，不纳入本轮重构。

## 4. 最终执行流

### 4.1 每小时维护

```text
FourLayerMemoryComp.CompTickRare
  -> parent.IsHashIntervalTick(TicksPerHour)
  -> MemorySummarizer.DailySummarize
  -> MemoryMaintainer.RunDecay
  -> MemoryMaintainer.ConvertActiveMemories
  -> MemoryMaintainer.CleanupLowActivityMemories
```

- [x] 每个 Pawn 哈希分散执行。
- [x] ABM 不做 Activity 衰减。
- [x] SCM、ELS、CLPA 使用各自衰减率。
- [x] 到期 ABM 中 `Action` 被移除，其他类型进入 SCM；所有条目先调用 `Privatize()`，其中 `RoundMemory` 生成保留 `OriginId` 的私有副本。
- [x] SCM、ELS、CLPA 清理 `Activity < 0.025` 的非 Pin 条目。

### 4.2 每日维护

```text
FourLayerMemoryComp.CompTickRare
  -> parent.IsHashIntervalTick(TicksPerDay)
  -> MemorySummarizer.PeriodicArchive
  -> MemoryMaintainer.EnforceMemoryLimits
```

- [x] 自动归档开关和每日总结开关相互独立。
- [x] 归档周期由 `ArchiveIntervalDays` 再次哈希判定。
- [x] SCM、ELS、CLPA 分别执行容量治理。
- [x] 容量计算只统计非 Pin 条目，Pin 条目允许列表总数超过配置值。

### 4.3 总结与归档

```text
MemorySummarizer
  -> 选择并排序源条目
  -> BuildPrompt / BuildEmptySummary
  -> 源对象加入 SummarizingMemories
  -> AIService.EnqueueAIRequest
  -> ClientPool fallback
  -> OpenAIClient
  -> 成功 callback：写入 ELS/CLPA，OriginId 加入 SummarizedIds
  -> dispose：源对象移出 SummarizingMemories
```

- [x] 自动总结选择 ABM + SCM 中未总结且早于最近完整周期的条目。
- [x] 自动总结按 key 降序提交，即最旧的完整周期先提交。
- [x] 手动总结允许已总结条目再次参与。
- [x] 自动归档选择全部未总结 ELS，不再按旧比例截取。
- [x] 手动归档只接受 `MemoryType.Summarization`。
- [x] AI 失败不产生目标条目。
- [x] ELS/CLPA 的 `GameTick` 取参与任务的最新源记忆时间，不使用 AI 响应完成时间。

AI 请求可并发执行，完成顺序不保证与提交顺序一致；但时间线和直接 ELS/CLPA API 输出按 `GameTick` 排序，因此回写先后不会造成 UI 时间线倒序。不同完成时点仍可能改变自动归档的批次边界。

### 4.4 AI 请求

- [x] `AIService.GameComponentUpdate()` 每帧检查 FIFO 队列。
- [x] 距上次启动不足 10 秒时不出队。
- [x] 单个 OpenAIClient 最多执行 3 次总尝试，等待 4 秒和 8 秒。
- [x] 429、5xx、连接超时和读取超时可重试。
- [x] 401、403 和其他 HTTP 错误立即结束当前客户端。
- [x] 当前客户端失败后由 ClientPool 尝试下一有效配置。
- [x] 网络轮询期间 `Current.Game == null` 时 Abort。
- [x] `finally` 始终执行业务 dispose。

## 5. 存档与兼容

- [x] 保留四层列表 Scribe key。
- [x] `MemoryEntry.Id` 和 `OriginId` 使用正 `long`；旧 `mem-` 十六进制 `Id` 在读档时转换。
- [x] `RoundMemory.Privatize()` 通过 `OriginId` 保留共享来源。
- [x] `SummarizingMemories` 不持久化，读档后为空。
- [x] `SummarizedIds` 作为 Pawn 私有集合随 `FourLayerMemoryComp` 持久化，保存前清理已不在四层仓库中的来源。
- [x] 旧存档缺少初始化标记时，将现有 ABM、SCM、ELS 一次性视为已处理，避免升级后重复总结历史记忆。
- [x] 保留 `PawnMemoryComp : FourLayerMemoryComp`。
- [x] 保留 `MemoryManager` 的 `lastDecayTick`、`commonKnowledge` 和 `promptCache`。
- [x] AI 配置使用 `UseRimTalkConfig`、`ApiConfigs`、`EnableAILog` 和 `ai_summaryMaxTokens` 等当前 key。
- [x] 不迁移已删除的旧独立 AI 配置字段。

## 6. 原路线图结项

### Phase 0：建立基线

- [x] 记录四层迁移、衰减、总结和归档的实际行为。
- [x] 保持关键 Scribe key 和旧存档默认值。
- [x] 将实现差异写入当前文档。

### Phase 1：抽取 MemoryMaintainer

- [x] 抽取衰减、清理和容量治理。
- [x] 抽取 Pin 与直接层级迁移。
- [x] 提供统一删除入口。
- [x] 保留 Comp 属性作为当前数据访问接口。

### Phase 2：整理写入边界

- [x] UI 删除迁移到 Maintainer。
- [x] UI 总结和归档迁移到 Summarizer。
- [x] 捕获、创建、导入继续使用 Comp 暴露的列表，作为最终兼容边界。
- [x] 取消“所有写入必须经 Maintainer”和“公开列表必须只读”的原设想，不再纳入本轮范围。

### Phase 3：抽取 MemorySummarizer

- [x] 统一每日、手动和批量总结。
- [x] 统一周期和手动归档。
- [x] 统一源选择、提示词和目标元数据。
- [x] 使用 AI 成功后落地、失败无目标的最终语义。
- [x] 使用闭包 callback 直接回写，取消独立任务版本模型的原设想。

### Phase 4：重构调度

- [x] 每日总结和自动归档拆成独立任务。
- [x] 从 `MemoryManager` 删除个人记忆调度。
- [x] 下沉至 `CompTickRare()` 并通过 Pawn hash 分散。
- [x] 使用 `AIService` 统一 AI 请求启动节奏。
- [x] 取消 `MemoryManager` 跨 Pawn 维护队列的原设想。

### Phase 5：重构 AI 请求与回写

- [x] 以 `Payload` 表达 AI 客户端结果。
- [x] 由 Summarizer 应用业务文本和源状态。
- [x] 用 dispose 统一从 `SummarizingMemories` 释放源对象。
- [x] 删除旧缓存、去重和回调注册表。
- [x] 使用当前闭包回写模型，取消 `targetMemoryId + requestId` 的原设想。

### Phase 6：下沉本地 Tick

- [x] 机械维护、每日总结和周期归档统一由 Pawn Comp 低频触发。
- [x] 删除重复的全局个人记忆衰减路径。
- [x] 维持当前实际 Tick 范围，不增加离图 Pawn 补偿调度。

### Phase 7：固定产品策略

- [x] ELS 超限采用直接淘汰，不自动迁入 CLPA。
- [x] CLPA 使用 `maxArchiveMemories` 容量治理，Pin 不计入裁剪数量。
- [x] Pin 保护优先于衰减、低活跃清理和容量淘汰。
- [x] SCM 保留为固定 ABM 和非 Action 到期 ABM 的过渡层。
- [x] `useAISummarization`、`maxSummaryLength` 作为兼容字段保留但不参与当前流程。
- [x] 自动总结和归档仅面向殖民者，不恢复声带链接动物逻辑。

## 7. 实现特征记录

以下内容是结项后的实际实现特征，不表示后续重构任务：

- 四层列表保持 public mutable `List<MemoryEntry>`。
- `MemoryMaintainer` 不承担捕获和导入写入。
- Summarizer 直接向 ELS/CLPA 添加结果。
- `SummarizingMemories` 只按对象引用防止 Summarizer 重复选中，不是机械维护保护锁。
- 在途 `RoundMemory` 私有化后，新副本不继承引用集合中的状态，理论上可被重复提交。
- `SummarizedIds` 由 Comp 按 Pawn 持久化，状态键为条目的 `OriginId`。
- 当前导入导出不包含 `SummarizedIds`，导入条目可能被重新总结或归档。
- AI 回写使用对象闭包，没有请求版本模型。
- AI 队列不持久化、不去重、无取消令牌。
- 10 秒是启动间隔，不是严格单并发限制。
- 自动总结按当前分组 key 降序执行，最旧完整周期优先。
- AI 完成顺序可与提交顺序不同；产物时间戳取源记忆的最新 `GameTick`，UI/API 再按该字段排序。
- `SummarizeAll` 按 SCM 是否非空预筛选 Pawn。
- 自动维护范围取决于 `ThingComp.CompTickRare()` 的实际 Tick 范围。

这些特征已在 `维护层.md` 和 `AI层重写方案.md` 中展开说明。

## 8. 结项标准

- [x] `FourLayerMemoryComp` 负责状态、存档和 Pawn 级调度。
- [x] `MemoryMaintainer` 集中机械维护规则。
- [x] `MemorySummarizer` 集中总结和归档规则。
- [x] `MemoryManager` 不再操作 Pawn 四层列表。
- [x] UI 不再实现独立总结或归档算法。
- [x] AI 基础设施不引用或修改 `MemoryEntry`。
- [x] 每日、手动和批量总结复用同一 Summarizer。
- [x] 自动归档和手动归档复用同一 Summarizer。
- [x] AI 配置、fallback、网络和重试集中在 `Source/AI/`。
- [x] 文档已按最终代码更新，维护层与 AI 层重构结项。

## 9. 一句话总纲

> `FourLayerMemoryComp` 持有单 Pawn 记忆并驱动低频维护，`MemoryMaintainer` 执行机械规则，`MemorySummarizer` 执行总结与归档，`AIService -> ClientPool -> OpenAIClient` 负责 AI 请求，`MemoryManager` 保留全局常识与缓存职责；以上结构即本轮重构的最终落地结果。
