# RimTalk Expand Memory - 维护层重构总纲

## 1. 文档定位

本文是个人记忆维护层重构的上位设计文档，用于统一以下内容：

- 维护层的目标边界与最终结构。
- 各组件的职责、所有权和依赖方向。
- 定时任务、UI 命令、语义聚合与 AI 回写的统一执行路径。
- 从当前实现迁移到目标结构的阶段顺序。
- 每阶段必须维持的存档兼容、行为基线和验收标准。

本文不替代具体模块的执行方案。各阶段应在实施前提供独立执行文档，并以本总纲作为约束来源。

当前下位文档：

- `MemoryMaintainer重构执行方案.md`：机械维护与直接迁移的首次抽取方案。
- 后续补充 `MemorySummarizer重构执行方案.md`。
- 后续补充 `维护调度与异步回写重构执行方案.md`。

## 2. 重构背景

当前维护层的核心能力已经存在，但代码所有权与执行入口不统一：

- `MemoryManager` 同时负责全局调度、Pawn 筛选和自动归档算法。
- `FourLayerMemoryComp` 同时持有数据、执行衰减、迁移、总结、归档和部分检索逻辑。
- UI 的 `AggregateMemories()` 直接实现另一套总结和归档流程。
- `IndependentAISummarizer` 同时承担 AI 基础设施、缓存、回调注册和业务结果传递。
- 四层列表以可变 `List<MemoryEntry>` 对外暴露，捕获、UI 和其他模块可以绕过维护规则直接修改。
- 小时容量治理、总结后裁剪和周期归档对 ELS 使用不同策略。
- 自动归档、每日总结和手动总结的触发条件存在非预期耦合。

这些问题的核心不是缺少功能，而是：

> 同一种状态变化可以由多个模块用不同规则完成，维护层没有唯一、稳定、可验证的写入路径。

## 3. 重构目标

### 3.1 核心目标

1. `FourLayerMemoryComp` 只承担 Pawn 记忆状态、存档和生命周期所有权。
2. `MemoryMaintainer` 统一机械维护与原子状态变更。
3. `MemorySummarizer` 统一总结、摘要式归档和语义压缩。
4. `MemoryManager` 只负责世界级时间、跨 Pawn 编排、队列和限流。
5. UI 只提交命令和展示结果，不实现维护算法。
6. AI 子系统只生成结果和派发完成事件，不直接决定记忆层级或源条目处置。
7. 所有记忆写操作最终经过受控入口，四层数据始终满足一致性约束。

### 3.2 工程目标

- 每个阶段均可独立编译、验证和回退。
- 结构抽取阶段默认保持当前行为，不夹带策略修正。
- 存档字段、Scribe key 和条目 ID 保持兼容。
- 定时任务可独立启停，不通过无关功能的开关间接控制。
- 异步结果可以识别过期、失效和重复请求。
- 维护行为能够按单 Pawn、单命令进行测试和诊断。

### 3.3 非目标

本轮维护层重构不负责：

- 重写捕获层或 RoundMemory 流式管线。
- 重写评分、向量检索和注入算法。
- 改变四层模型的产品定义。
- 一次性清除全部旧存档兼容代码。
- 为每条维护规则创建独立 Service。
- 在结构抽取期间重新设计全部衰减率、容量和归档比例。

## 4. 目标架构

```text
外部触发
  ├─ MemoryManager：时间任务、跨 Pawn 队列
  ├─ FourLayerMemoryComp：Pawn 低频 Tick
  ├─ UI：用户命令
  └─ 捕获层：新增记忆
          |
          v
FourLayerMemoryComp (状态与门面)
  ├─ 四层记忆数据
  ├─ 存档读写
  ├─ 查询与只读暴露
  ├─ JobMemoryCapturer
  ├─ MemoryMaintainer
  └─ MemorySummarizer
          |
          +-------------------------+
          |                         |
          v                         v
MemoryMaintainer              MemorySummarizer
  - 原子插入/删除/移动          - ABM/SCM -> ELS
  - Pin 与直接迁移              - ELS -> CLPA 摘要归档
  - Activity 衰减               - 源条目选择与分组
  - 低活跃清理                  - 占位摘要与元数据继承
  - 容量与一致性治理            - 提交 AI 总结任务
          ^                         |
          |                         v
          +---- 提交状态变更 ---- AI Summary Infrastructure
                                    - 请求与供应商适配
                                    - 去重与缓存
                                    - 完成结果排队
                                    |
                                    v
                              主线程结果派发
                                    |
                                    v
                         MemorySummarizer.ApplyResult
```

## 5. 组件职责

### 5.1 FourLayerMemoryComp

定位：单个 Pawn 个人记忆的状态所有者和稳定门面。

负责：

- 持有 ABM、SCM、ELS、CLPA 四层集合。
- 负责 Scribe 存档读写和读档后空集合修复。
- 持有 `JobMemoryCapturer`、`MemoryMaintainer`、`MemorySummarizer` 实例。
- 提供查询、只读视图和必要的兼容入口。
- 接收 Pawn 生命周期事件和低频 Tick。
- 将外部命令转发给对应协作对象。

不负责：

- 实现衰减、容量治理和层级迁移算法。
- 实现总结、归档摘要和 AI 提示词逻辑。
- 遍历其他 Pawn。
- 处理全局队列和跨 Pawn 限流。

### 5.2 MemoryMaintainer

定位：绑定单个 `FourLayerMemoryComp` 的机械维护器和原子状态变更执行器。

负责：

- 按层衰减 `Activity`。
- 清理低活跃非固定记忆。
- 执行各层容量策略。
- 处理 Pin 状态和 Pin 导致的直接迁移。
- 处理原条目的插入、删除、移动和按时间排序。
- 校验并修复列表归属与 `MemoryEntry.Layer` 的一致性。
- 为 `MemorySummarizer` 提供提交聚合结果所需的原子操作。

不负责：

- 选择需要总结的源条目。
- 生成摘要文本。
- 选择 LLM 提示词或供应商。
- 决定每日任务和归档周期。
- 发送 UI 消息。

### 5.3 MemorySummarizer

定位：绑定单个 `FourLayerMemoryComp` 的语义总结器。

负责：

- 统一每日、批量和选中总结。
- 统一自动和选中归档。
- 按命令指定范围选择源条目，避免扩大删除范围。
- 决定分组方式、目标层和目标类型。
- 创建立即可用的简单摘要条目。
- 合并时间戳、重要性、标签、关键词和关联信息。
- 通过 `MemoryMaintainer` 原子提交源条目处置和目标条目插入。
- 提交 AI 请求，并在主线程接收结果后更新目标条目。
- 验证目标条目和请求版本，拒绝过期回写。

不负责：

- 网络请求实现。
- AI 供应商配置读取。
- 世界级时间调度。
- UI 选择状态和确认框。
- 直接操作四层列表。

### 5.4 MemoryManager

定位：世界级维护任务编排器。

负责：

- 冷启动缓冲。
- 每日总结、周期归档等全局时间判断。
- 维护任务开关的独立判断。
- 枚举符合条件的 Pawn。
- 跨 Pawn 队列、去重、限流和取消。
- 调用 `FourLayerMemoryComp` 的稳定命令入口。
- 保存必须跨存档延续的调度游标。

不负责：

- 读取或修改某个 Pawn 的四层列表。
- 创建 ELS/CLPA 条目。
- 生成摘要或注册业务回调。
- 实现单 Pawn 衰减和容量算法。

### 5.5 AI Summary Infrastructure

当前由 `IndependentAISummarizer` 与 `AIRequestManager` 承担，重构后定位为 AI 基础设施。

负责：

- 初始化供应商、模型和认证配置。
- 构造并发送 AI 请求。
- 请求去重、并发控制和有限结果缓存。
- 将完成结果安全送回主线程。
- 返回成功、失败、取消和超时状态。

不负责：

- 直接捕获并修改 `MemoryEntry`。
- 决定源条目是否删除。
- 决定结果写入 ELS 还是 CLPA。
- 添加"AI总结""待AI更新"等业务标签。

### 5.6 UI

定位：命令发起和结果展示层。

负责：

- 收集 Pawn、条目 ID 和用户选项。
- 显示确认、进度、成功和失败信息。
- 在命令完成后刷新视图缓存。

不负责：

- 聚合、迁移和删除源条目。
- 创建总结或归档条目。
- 直接注册 AI 回调。
- 复制维护层的 Pin、容量和保护规则。

## 6. 依赖方向

允许的依赖：

```text
MemoryManager -> FourLayerMemoryComp public facade
UI            -> FourLayerMemoryComp public facade
Capture       -> FourLayerMemoryComp write facade

FourLayerMemoryComp -> MemoryMaintainer
FourLayerMemoryComp -> MemorySummarizer

MemorySummarizer -> MemoryMaintainer
MemorySummarizer -> AI summary interface

AIRequestManager -> AI result queue/dispatcher
```

禁止的依赖：

```text
MemoryMaintainer -X-> MemoryManager
MemoryMaintainer -X-> UI
MemoryMaintainer -X-> IndependentAISummarizer

MemorySummarizer -X-> MemoryManager
MemorySummarizer -X-> UI

UI -X-> mutable ABM/SCM/ELS/CLPA lists
MemoryManager -X-> mutable ABM/SCM/ELS/CLPA lists
AI infrastructure -X-> MemoryEntry object mutation
```

组件之间不通过静态全局字段传递某个 Pawn 的业务状态。Pawn 级逻辑通过所属 Comp 实例关联，异步任务通过稳定 ID 关联。

## 7. 统一命令模型

维护层最终应通过明确命令表达"触发来源"和"操作范围"，而不是根据调用方法猜测语义。

建议概念模型：

```csharp
public enum MemoryOperationTrigger
{
    Scheduled,
    User,
    CapacityPolicy,
    Migration
}

public sealed class SummarizationRequest
{
    public MemoryLayer SourceLayer;
    public MemoryLayer TargetLayer;
    public IReadOnlyList<string> SourceMemoryIds;
    public MemoryOperationTrigger Trigger;
    public string PromptTemplate;
}
```

不要求第一阶段立即引入这些类型。原则是：

- 选中操作只能影响请求中明确列出的 ID。
- 全层操作必须显式构造全层 ID 集合。
- 定时任务和用户命令使用同一个执行器，仅触发来源不同。
- 执行结果应返回处理数、跳过数和失败原因，UI 不自行推测结果。

## 8. 状态一致性约束

维护层必须始终满足以下不变量：

1. 同一 `MemoryEntry` 引用最多存在于一个层级列表。
2. `MemoryEntry.Layer` 与实际所在列表一致。
3. 同一个 Comp 内，非空 `MemoryEntry.Id` 必须唯一。
4. Pin 条目不会被自动衰减、低活跃清理或容量淘汰。
5. 自动操作不得扩大到命令范围之外的条目。
6. 聚合目标条目成功插入后，才能提交源条目删除或迁移。
7. AI 失败不产生副作用：不创建目标条目，不修改源条目状态（`IsSummarized` 不被设置），源条目可在下一轮再次参与总结。
8. AI 回写不得复活已删除条目或覆盖更新版本更高的条目。
9. 读档后不得因为协作对象重建而改变四层数据。
10. 所有自动维护操作必须在 RimWorld 主线程提交状态变化。当前实现中，`async void` 的 `await` 续体通过 Unity 的 `UnitySynchronizationContext` 自动回到主线程，无需手动 marshal。

开发模式可提供低频一致性检查，但正式运行路径不应每 tick 全量扫描四层列表。

## 9. 核心执行流

### 9.1 本地机械维护

```text
FourLayerMemoryComp.CompTickRare (每 Pawn 哈希分散)
  每小时 (IsHashIntervalTick TicksPerHour):
    -> MemorySummarizer.DailySummarize (总结检查)
    -> MemoryMaintainer.RunDecay
    -> MemoryMaintainer.ConvertActiveMemories
    -> MemoryMaintainer.CleanupLowActivityMemories
  每天 (IsHashIntervalTick TicksPerDay):
    -> MemorySummarizer.PeriodicArchive (归档检查)
    -> MemoryMaintainer.EnforceMemoryLimits
```

当前实现已将机械维护和总结/归档触发统一下沉到 `CompTickRare`，`MemoryManager` 不再参与调度。

### 9.2 每日总结

```text
FourLayerMemoryComp.CompTickRare (每小时)
  -> MemorySummarizer.DailySummarize
      -> 校验配置、间隔、上次总结 tick
      -> AutoSummarize
          -> 选择 ABM/SCM 中 IsSummarizing=false && IsSummarized=false 的条目
          -> 按天分组，最旧周期先提交
          -> SummarizeInternal
              -> BuildPrompt (构建提示词)
              -> BuildEmptySummary (创建空目标条目)
              -> 标记源条目 IsSummarizing=true
              -> AIRequestManager.EnqueueAIRequest (入列)
```

### 9.3 周期归档

```text
FourLayerMemoryComp.CompTickRare (每天)
  -> MemorySummarizer.PeriodicArchive
      -> 校验配置、间隔
      -> Archive
          -> 选择 ELS 中 IsSummarizing=false && IsSummarized=false 的条目
          -> ArchiveInternal
              -> BuildPrompt (构建提示词)
              -> BuildEmptySummary (创建空目标条目)
              -> 标记源条目 IsSummarizing=true
              -> AIRequestManager.EnqueueAIRequest (入列)
```

每日总结和周期归档是平级任务，各自读取自己的开关和周期，由 `CompTickRare` 的不同 `IsHashIntervalTick` 间隔触发。

### 9.4 UI 选中操作

```text
UI
  -> 收集 selected memory IDs
  -> FourLayerMemoryComp.Summarizer.ManualSummarize(ids) / Archive(ids)
  -> 与定时任务相同的 Summarizer 内部路径
  -> UI 刷新与提示
```

UI 不再拥有 `AggregateMemories()` 形式的业务实现。

### 9.5 AI 结果回写

```text
AIRequestManager.WorldComponentUpdate (每帧)
  -> 每 10 秒出队一个请求
  -> ExecuteTask (async void)
      -> await IndependentAISummarizer.CallAIAsync(prompt)
      -> UnitySynchronizationContext 回到主线程
      -> 检查 Current.Game != null
      -> 回调:
          -> AI 成功: 设置 Content, 添加到目标列表, 标记源条目 IsSummarized=true
          -> AI 失败: Log.Error, return (不创建条目)
          -> finally: 重置源条目 IsSummarizing=false
```

当前实现尚未引入 `targetMemoryId + requestId` 版本机制，回调通过闭包直接捕获目标条目和源条目列表。目标条目删除后的安全性依赖 `Current.Game` 检查和实例级队列生命周期。

## 10. Tick 与生命周期策略

### 10.1 全局调度适用范围

以下任务原计划保留在 `MemoryManager`，但当前实现已将总结和归档调度下沉到 `CompTickRare`，`MemoryManager` 仅保留冷启动缓冲和常识更新：

- ~~每日一次或每若干天一次的世界时间任务。~~ 已下沉到 `CompTickRare` + `IsHashIntervalTick`。
- ~~需要遍历多个 Pawn 的批处理。~~ "一键总结所有殖民者"由 UI 直接遍历 Pawn 调用 `AutoSummarize`。
- ~~需要跨 Pawn 限流的总结和归档。~~ 由 `AIRequestManager` 的 10 秒请求间隔承担。
- 离图 Pawn 的补偿维护，如产品规则明确要求。（尚未实现）

若未来需要世界级时间任务或离图 Pawn 补偿维护，可重新在 `MemoryManager` 中引入。

### 10.2 Pawn Tick 适用范围

以下任务可以由 `FourLayerMemoryComp` 的低频 Tick 触发：

- 单 Pawn Activity 衰减。
- 低活跃清理。
- 本地容量和一致性治理。

约束：

- 不在普通 `CompTick()` 中全量扫描。
- 优先使用低频 Tick 或哈希分散执行。
- 不能同时存在两个未去重的衰减入口。
- 必须明确生成中、离图、商队和世界 Pawn 的维护语义。

### 10.3 协作对象生命周期

`MemoryMaintainer` 和 `MemorySummarizer` 默认是无持久状态 POCO：

- 在 `FourLayerMemoryComp` 构造时创建。
- 不单独写入 Scribe。
- 读档后继续引用同一个 Comp 中恢复的四层数据。
- 需要持久化的任务状态应放在 Comp、WorldComponent 或专门任务记录中，而不是隐藏在协作对象字段里。

## 11. 异步任务模型

后续 AI 重构建议引入最小任务标识：

```text
requestId
pawnThingId
targetMemoryId
sourceMemoryIds
operationType
promptTemplate
createdTick
status
```

最低要求：

- 同一目标的新请求可以使旧请求失效。
- 目标条目删除后，返回结果被安全丢弃。
- 请求失败时移除"待 AI 更新"状态并保留简单摘要。
- 回调异常不会阻塞其他主线程回调。
- 缓存 key 包含任务类型或模板身份，避免同一源集合的日常总结与深度归档相互命中。

是否持久化进行中的 AI 任务由后续执行方案决定。默认允许读档后放弃未完成网络请求，但不能留下永久"处理中"的业务状态。

## 12. 配置归属

配置仍集中存放于 `RimTalkSettings`，但按消费者明确归属：

| 配置类别 | 消费者 |
|---|---|
| SCM/ELS/CLPA 衰减率 | `MemoryMaintainer` |
| 各层容量和清理阈值 | `MemoryMaintainer` |
| 每日总结开关和小时 | `MemoryManager` |
| 自动归档开关和间隔 | `MemoryManager` |
| 总结源条目选择规则 | `MemorySummarizer` |
| 提示词和最大 token | AI summary infrastructure / Summarizer 请求 |
| AI 供应商和认证信息 | AI summary infrastructure |

设置 UI 只编辑配置，不直接改变维护算法的执行路径。

## 13. 重构路线图

### Phase 0：建立基线

- [ ] 记录当前四层迁移、衰减、总结和归档行为。
- [ ] 建立关键存档兼容样本。
- [ ] 标记当前已知缺陷与当前既有行为的区别。
- [ ] 为每个后续阶段确定最小回归清单。

### Phase 1：抽取 MemoryMaintainer

执行依据：`MemoryMaintainer重构执行方案.md`。

- [x] 抽取衰减、清理和容量治理。
- [x] 抽取 Pin 与直接层级迁移。
- [x] 建立插入、删除、移动原语。
- [x] 保留 Comp 兼容入口和当前调度。
- [x] 不修改总结与 AI 行为。

### Phase 2：建立受控写入边界

- [ ] 增加 Comp 内部写入门面。
- [ ] 将捕获层直接 `List.Add` 改为受控添加入口。
- [ ] 将 UI 直接删除、移动改为命令入口。
- [ ] 将公开可变列表逐步收敛为只读视图。
- [ ] 加入 ID 唯一性和 Layer 一致性检查。

该阶段完成后，外部模块不得直接修改四层列表。

### Phase 3：抽取 MemorySummarizer

- [x] 统一 `DailySummarization()` 与 `ManualSummarization()`。
- [x] 迁移 UI `AggregateMemories()`。
- [x] 迁移 `MemoryManager.CheckArchiveInterval()` 中的 Pawn 级归档算法。
- [x] 统一源条目范围、分组、目标条目和元数据继承。
- [ ] 所有状态提交通过 Maintainer 原子操作完成。

> **实际执行偏差**
>
> - 新增 `MemoryType.Summarization` 替代旧的 `Conversation` 作为总结产物类型。
> - 新增 `IsSummarizing` 非持久字段，防止同一批次条目被并发选中。
> - 移除 `CanBeSummarized` 属性，筛选条件内联到 `MemorySummarizer`。
> - **AI 失败设计变更**：不再"先落地简单摘要再 AI 覆盖"，改为"AI 成功才创建条目"。AI 失败时不产生副作用，源条目可在下一轮再次参与总结。这使约束 #7 的含义从"不影响简单摘要可用性"变为"不产生副作用"。
> - **源条目处理变更**：总结/归档后不再清空源条目，仅标记 `IsSummarized = true`，源条目依靠 `ConvertActiveMemories` 和 `CleanupLowActivityMemories` 自然消亡。
> - **未完成**：状态提交未通过 `MemoryMaintainer` 原子操作完成，`SummarizeInternal` 直接操作 `List<MemoryEntry>`。待后续阶段补齐。
> - **声带链接动物暂时排除**：因 Comp 注入时机问题（Hediff 在 Comp 初始化时尚未初始化），`IsColonyAnimalWithVocalLink` 逻辑无效，暂时移除。待 Comp 注入问题解决后重启。

### Phase 4：重构全局调度

- [x] 将每日总结和自动归档拆成平级任务。
- [x] 修正各自开关和周期的独立性。
- [ ] 统一队列任务模型、Pawn 去重、取消和失效检查。
- [x] 删除 `MemoryManager` 中的摘要生成和条目操作代码。

> **实际执行偏差**
>
> - **调度方式变更**：原计划保留 `MemoryManager` 的队列调度，实际实现将总结和归档触发完全下沉到 `CompTickRare`，通过 `IsHashIntervalTick` 哈希分散。`MemoryManager` 不再持有任何总结/归档队列。
> - **跨 Pawn 限流变更**：原计划由 `MemoryManager` 统一队列限流，实际由 `AIRequestManager` 的 10 秒请求间隔承担。Pawn 级触发不再有去重队列，每个 Pawn 独立触发。
> - **未完成**：`MemoryManager` 级的统一队列任务模型未实现。若未来需要更精细的跨 Pawn 去重和取消，需在 `MemoryManager` 或 `AIRequestManager` 层面重新引入。

### Phase 5：重构 AI 请求与回写

- [ ] AI 基础设施返回结构化结果。
- [ ] 用 `targetMemoryId + requestId` 替代捕获 `MemoryEntry` 的闭包。
- [x] 由 Summarizer 应用业务标签和文本。
- [x] 明确失败、取消、读档和目标删除后的处理。
- [x] 修正不同任务类型的缓存 key 隔离。

> **实际执行偏差**
>
> - **已完成**：`IndependentAISummarizer` 简化为纯 AI 调用基础设施，移除了缓存（`completedSummaries`）、去重（`pendingSummaries`）、回调注册（`callbackMap`）和提示词构建（`BuildPrompt`）。提示词构建迁移到 `MemorySummarizer.BuildPrompt`。缓存 key 隔离问题随缓存移除而消除。
> - **已完成**：`AIRequestManager` 新增实例级请求队列 + 10 秒限流 + `Current.Game` 检查。AI 失败时不创建条目（无副作用设计）。读档后放弃未完成请求，不留下永久"处理中"状态（`IsSummarizing` 非持久，读档后重置为 `false`）。
> - **已完成**：线程安全由 Unity 的 `UnitySynchronizationContext` 保证，`async void` 的 `await` 续体自动回到主线程，无需手动 marshal。
> - **未完成**：AI 基础设施仍返回 `Task<string>` 而非结构化结果（含状态、取消、超时信息）。
> - **未完成**：回调仍通过闭包捕获 `MemoryEntry` 实例，未引入 `targetMemoryId + requestId` 版本机制。目标条目在 AI 在途期间被删除的安全性依赖 `Current.Game` 检查和实例级队列生命周期，而非显式版本校验。

### Phase 6：下沉本地 Tick

- [ ] 确认全部目标 Pawn 的 Comp 挂载与 Tick 范围。
- [ ] 使用低频或哈希 Tick 分散本地维护。
- [ ] 为离图 Pawn 确定补偿策略。
- [ ] 移除重复的全局衰减路径。
- [ ] 对比重构前后的每小时耗时峰值。

### Phase 7：统一产品策略

此阶段才处理行为层面的冲突：

- [ ] 统一 ELS 超限应删除、直接迁移还是摘要归档。
- [ ] 让 CLPA 上限成为明确且持续的约束，或正式声明无上限。
- [ ] 明确 Pin、用户编辑和已总结状态的保护优先级。
- [ ] 明确 SCM 在当前产品中的长期定位。
- [ ] 清理无效配置，如确认未使用的 `maxSummaryLength`。
- [ ] 更新设置说明、维护层文档和迁移提示。

## 14. 迁移原则

### 14.1 结构与行为分离

一次提交应尽量只做以下一种事情：

- 移动代码所有权但保持行为。
- 修改调用路径但保持结果。
- 修复一个已确认缺陷。
- 修改一项产品策略并更新测试与文档。

不得在大型抽取提交中顺带调整衰减率、容量、归档比例或 Pin 语义。

### 14.2 兼容入口逐步收口

早期允许保留以下薄委托：

```csharp
FourLayerMemoryComp.DecayActivity()
FourLayerMemoryComp.PinMemory(...)
```

`FourLayerMemoryComp.DailySummarization()` 已在 Phase 3 中删除，外部调用方已迁移到 `Summarizer` 入口。

调用方迁移稳定后，再决定删除、内部化或保留为正式门面。不要同时要求全部外部模块直接依赖 Maintainer 或 Summarizer。

### 14.3 不引入无依据的兼容层

需要保留的兼容对象：

- 已写入存档的字段和 Scribe key。
- 已存在的 `PawnMemoryComp : FourLayerMemoryComp` 类型关系。
- 可能被外部模组调用的公共入口，在确认消费者前不删除。

不需要保留的内容：

- 无调用的私有重复实现。
- UI 内部重复的总结算法。
- 仅用于过渡且不存在外部消费者的新旧双路径。

## 15. 测试与质量门禁

### 15.1 每阶段必做

- [ ] 修改前对目标符号执行影响分析。
- [ ] 构建项目且禁用自动部署。
- [ ] 执行 `git diff --check`。
- [ ] 检查未预期的存档字段变化。
- [ ] 检查维护操作是否仍只在主线程提交。
- [ ] 检查同一记忆是否出现跨层重复。
- [ ] 提交前执行 GitNexus `detect_changes()`。

构建命令：

```powershell
dotnet build .\RimTalk-ExpandMemory.csproj -p:BuildingWithScript=true
```

### 15.2 核心回归矩阵

| 场景 | 必须验证 |
|---|---|
| 旧存档读取 | 四层数量、ID、Layer、Activity、Pin 不变 |
| 新增 ABM | 只进入目标 Pawn，时间戳与类型正确 |
| 小时衰减 | ABM 不变，其余层按设置衰减 |
| 低活跃清理 | 只清理符合规则的非固定条目 |
| 容量治理 | 不误删 Pin，不产生跨层重复 |
| Pin ABM | 进入 SCM，原层移除，字段保留 |
| Pin RoundMemory | 实体化成功，原引用删除 |
| 每日总结 | 源范围正确，只生成一次 ELS |
| 选中总结 | 只处置选中的条目 |
| 自动归档 | 开关独立，源范围和比例正确 |
| AI 成功 | 主线程更新正确目标条目 |
| AI 失败 | 简单摘要保留，状态可恢复 |
| AI 延迟返回 | 不覆盖已删除或更新版本更高的条目 |
| 多地图/商队 | 不重复维护，不遗漏约定范围 |

### 15.3 性能门禁

- 不允许每 Pawn 每 tick 全量扫描四层列表。
- 不允许同一小时同时执行全局衰减和 Pawn 本地衰减。
- 跨 Pawn 总结继续限流，不能在同一 tick 集中发起全部 AI 请求。
- 一致性检查在正式模式下应按需或低频执行。
- 缓存和任务表必须有明确上限或淘汰策略。

## 16. 文档体系

维护层文档按以下层级维护：

```text
维护层.md
  - 当前已落地架构与实际行为

维护层重构总纲.md
  - 目标架构、依赖规则和总体路线

MemoryMaintainer重构执行方案.md
MemorySummarizer重构执行方案.md
维护调度与异步回写重构执行方案.md
  - 各阶段的具体改动、顺序和验证清单
```

规则：

- `维护层.md` 只描述当前已经实现的事实，不提前宣称目标架构已完成。
- 本总纲记录长期目标和阶段约束。
- 执行文档记录具体改动步骤，并在完成后标记实际结果和偏差。
- 代码行为变化后，应在同一阶段更新 `维护层.md`。

## 17. 最终完成标准

维护层整体重构完成时，应同时满足：

1. `FourLayerMemoryComp` 是状态所有者，不再包含大段维护或总结算法。
2. `MemoryMaintainer` 是机械维护和原子状态变更的唯一实现位置。
3. `MemorySummarizer` 是总结和摘要式归档的唯一实现位置。
4. `MemoryManager` 只负责任务编排，不操作 Pawn 内部列表。
5. UI 不创建、迁移或批量删除记忆条目。
6. AI 基础设施不直接修改 `MemoryEntry`。
7. 外部模块无法绕过受控入口直接修改四层集合。
8. 每日总结、自动归档和手动命令使用同一业务执行器。
9. 全局任务开关互不依赖，Tick 路径没有重复执行。
10. 存档兼容、主线程安全、异步失效和性能门禁均通过验证。

## 18. 一句话总纲

> `FourLayerMemoryComp` 持有单 Pawn 记忆状态，`MemoryMaintainer` 维护状态健康并执行原子迁移，`MemorySummarizer` 完成语义压缩，`MemoryManager` 负责编排，UI 只发命令，AI 子系统只生成并派发结果；所有记忆变化最终通过受控的单一写入路径提交。
