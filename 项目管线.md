# RimTalk Expand Memory - 项目架构与运作管线

在深入遍历了项目的各个核心模块之后，结合最新的 v5.x 架构演进，以下为您修正并完善的当前项目运作管线示意图。

根据实际架构设计，**常识系统（WorldComponent）**、**轮次记忆系统（GameComponent）** 与 **个人记忆系统（ThingComp）** 构成了游戏内部三大不同生命周期层级的基石。它们在各自的生命周期层级独立收集、存储、衰减数据，并最终**在 API 注入端汇聚**。

## 1. 系统运作管线示意图

```text
========================================================================================
                              【RimTalk 扩展记忆与常识管线】
========================================================================================

【子系统 A：记忆管线 (基于单体 Pawn 的经历)】          【子系统 B：常识管线 (基于全局 World 的背景)】

  [外部触发源] (对话/原版事件/Job等)                       [玩家手动添加] / [事件生成器]
       │                                                      │
       ▼                                                      ▼
 ┌───────────────┐                                    ┌───────────────┐
 │ 捕获 (Capture)│                                    │ 常识库 (KB)   │
 │ Patches 拦截层│                                    │ Common        │
 └───────┬───────┘                                    │ Knowledge     │
         │                                            │ Library       │
         ▼                                            └───────┬───────┘
 ┌───────────────┐                                            │
 │ 轮次记忆缓存  │ (GameComponent: RoundMemoryManager)        │
 │(RimRingBuffer)│ -> 跨Pawn复用、防重复的对话高频缓存        │
 └───────┬───────┘                                            │
         │                                                    │
         ▼                                                    │
======================================                        │
 【记忆管理层 (四层模型)】                                    │
 (挂载于 Pawn 的 FourLayerMemoryComp)                          │
======================================                        │
         │                                                    │
    ┌────┴────┐ (无上限)                                       │
    │   ABM   │ ─┐ (接收最新 MemoryEntry / RoundMemory 指针)  │
    └────┬────┘  │ [固定]                                     │
         │       ▼                                            │
         │  ┌─────────┐                                       │
         │  │   SCM   │ ─┐                                    │
         │  └─────────┘  │                                    │
 [每日总结]│       │        │                                    │
         ▼       ▼        │                                   │
    ┌───────────────┐    │                                   │
    │      ELS      │ ───┘                                   │
    └────────┬──────┘                                        │
             │ [衰减归档]                                    │
             ▼                                               │
    ┌───────────────┐                                        │
    │     CLPA      │                                        │
    └───────────────┘                                        │
             │                                               │
             │                                               │
             ▼                                               ▼
========================================================================================
                          【动态评估与组装层 (Scoring & Assembly)】
========================================================================================
 ┌───────────────┐                                    ┌───────────────┐
 │ 统一注入调度  │ (UnifiedMemoryInjector)            │ 知识检索与评分│ (匹配与向量查询)
 │ + 配额收割    │ (ABM/ELS Collector)                │ + 向量DB同步  │ (VectorService)
 └───────┬───────┘                                    └───────┬───────┘
         │                                                    │
         └─────────────────────────┬──────────────────────────┘
                                   │
                                   ▼
========================================================================================
                          【API 注入端 (API Integration)】
========================================================================================
                                   │
 ┌─────────────────────────────────┴─────────────────────────────────┐
 │ RimTalkAPIIntegration (利用反射机制，主动向模板引擎注册变量)        │
 │ SimpleRimTalkIntegration (提供公有的静态方法供旧版 RimTalk 回退调用)│
 ├─────────────────────────────────┬─────────────────────────────────┤
 │ MemoryVariableProvider          │ KnowledgeVariableProvider       │
 │ 暴露 {{pawn.memory}}            │ 暴露 {{knowledge}}              │
 │ 内部调用 UnifiedMemoryInjector    │ 内部调用 CommonKnowledgeLibrary │
 └─────────────────────────────────┴─────────────────────────────────┘
                                   │
                                   ▼
                          [RimTalk LLM 引擎]
```

---

## 2. 核心中的核心：三大维度的数据基石

除了我们前面提到的 **`MemoryManager` (WorldComponent 记忆管理器)**、**`FourLayerMemoryComp` (ThingComp 四层组件)** 以及 **`MemoryEntry` (记忆条目)** 之外，系统中还存在与它们**完全同级**的四大核心类，它们共同支撑起了游戏的三个维度（全局世界、当前存档、单体角色）：

### 🥇 维度 1：Game 级别 (贯穿整个当前存档)
应对超高频的对话流，需要一个不依赖具体某个小人的中枢。
*   **`RoundMemoryManager` (GameComponent)**:
    *   **同级地位**：与 `MemoryManager` 级别等同，但它挂载在 `Game` 上。它负责全局的“轮次对话中控台”，使用 `RimRingBuffer` 维护一个高效定长队列。在玩家读档后，它会主动去遍历所有小人，做一次惊人的“狸猫换太子”（指针替换），使得一场群体对话无论涉及多少个小人，在内存里只占用一份 `RoundMemory` 实例，大幅缩减存档体积。
*   **`RoundMemory`**:
    *   **同级地位**：继承自 `MemoryEntry` 的特化数据类。普通的 `MemoryEntry` 是单人的回忆，而 `RoundMemory` 带有 `HashSet<Pawn> Pawns`（参会人员）和 `AbsTick`（绝对游戏时间），代表“大家共同经历的同一场对话”。

### 🥇 维度 2：World 级别 (贯穿世界的背景与常识)
*   **`MemoryManager` (WorldComponent)**:
    *   **同级地位**：全局衰减与总结的心跳控制器，持有 `CommonKnowledgeLibrary`。
*   **`CommonKnowledgeLibrary`**:
    *   **同级地位**：它是与 `FourLayerMemoryComp` (四层容器) 对标的常识大总管。存储了世界上所有不随单个小人改变而改变的规则与设定。
*   **`CommonKnowledgeEntry`**:
    *   **同级地位**：与 `MemoryEntry` 同级的数据模型。带有 `KnowledgeEntryCategory` (类别) 和 `KeywordMatchMode` (匹配模式)，用来告诉系统这段背景故事该在什么时候被 LLM 唤起。

### 🥇 维度 3：Pawn 级别 (绑定在单个角色的肉体上)
*   **`FourLayerMemoryComp` (ThingComp)**:
    *   **同级地位**：单体记忆的调度和控制中枢。
*   **`MemoryEntry`**:
    *   **同级地位**：个体的单条记忆承载单元。

---

## 3. 全项目文件归属模块清单 (按独立系统划分)

### 📌 A. 个人记忆系统 (Memory System) - 挂载于个体 (Pawn)
*   **拦截与捕获**:
    *   `Source/Patches/TalkHistory_AddMessageHistory_Patch.cs`
    *   `Source/Patches/JobMemoryPatch.cs`, `IncidentPatch.cs`, `RimTalkConversationCapturePatch.cs`
    *   `Source/Patches/CustomDialogueService_ExecuteDialogue.cs`
    *   `Source/Patches/RimChat/*` (兼容第三方对话模组)
*   **模型与存储 (核心基石)**:
    *   `Source/Memory/FourLayerMemoryComp.cs` (单人容器)
    *   `Source/Memory/MemoryTypes.cs` (`MemoryEntry`, `MemoryLayer`)
    *   `Source/Memory/PawnMemoryCompAlias.cs`
*   **处理与收集 (Collectors)**:
    *   `Source/Memory/Injection/ABMCollector.cs`, `ELSCollector.cs`
*   **动态评分与调度**:
    *   `Source/Memory/Injection/UnifiedMemoryInjector.cs`
    *   `Source/Memory/DynamicMemoryInjection.cs`
    *   `Source/Memory/SemanticScoringSystem.cs`, `AdvancedScoringSystem.cs`, `SceneAnalyzer.cs`

### 📌 B. 轮次对话系统 (Round Memory) - 挂载于当前存档 (Game)
*   **模型与存储 (核心基石)**:
    *   `Source/Memory/RoundMemory/RoundMemoryManager.cs` (游戏全局缓冲池)
    *   `Source/Memory/RoundMemory/RoundMemory.cs` (特化型多人群聊记忆模型)
    *   `Source/Utils/RimRingBuffer.cs`

### 📌 C. 常识世界系统 (Knowledge System) - 挂载于世界 (World)
*   **模型与管理 (核心基石)**:
    *   `Source/Memory/MemoryManager.cs` (世界全局载体)
    *   `Source/Memory/CommonKnowledgeLibrary.cs` (常识规则容器)
    *   `Source/Memory/KnowledgeTypes.cs` (包含 `CommonKnowledgeEntry`)
    *   `Source/Memory/ExtendedKnowledgeEntry.cs`
*   **自动生成与获取**:
    *   `Source/Memory/EventRecordKnowledgeGenerator.cs`
    *   `Source/Memory/PawnStatusKnowledgeGenerator.cs`
*   **向量搜索支持**:
    *   `Source/Memory/VectorDB/VectorService.cs`
    *   `Source/Memory/VectorDB/InMemoryVectorStore.cs`

### 📌 D. API 注入与集成 (API Integration) - 向外输出
*   **接口注册**:
    *   `Source/API/RimTalkAPIIntegration.cs`
    *   `Source/Patches/SimpleRimTalkIntegration.cs`
*   **数据提供者 (Providers)**:
    *   `Source/API/MemoryVariableProvider.cs`
    *   `Source/API/KnowledgeVariableProvider.cs`

### 🛠️ E. 通用基础设施与 UI (Shared & UI)
*   **文本清理与缓存**:
    *   `Source/Memory/PromptNormalizer.cs`
    *   `Source/Memory/ContextCleaner.cs`, `SuperKeywordEngine.cs`, `KeywordExtractionHelper.cs`
    *   `Source/Memory/ConversationCache.cs`, `PromptCache.cs`
*   **交互界面 (UI)**:
    *   `Source/Memory/UI/MainTabWindow_Memory.cs` 及其诸多子面板
    *   `Source/Settings/SettingsUIDrawers.cs`
*   **大模型服务**:
    *   `Source/Memory/AI/IndependentAISummarizer.cs`
    *   `Source/Memory/AI/AIRequestManager.cs`, `EmbeddingService.cs`