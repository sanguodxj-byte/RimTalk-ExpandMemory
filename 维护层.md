# RimTalk Expand Memory - 维护层当前实现

> 本文描述重构结束后的当前维护行为。重构结项记录见 `维护层重构总纲.md`。

## 1. 边界与核心组件

维护层位于捕获与检索/注入之间，负责：

- 四层个人记忆的持有和存档。
- Activity 衰减、ABM 到期处理、低活跃清理和容量裁剪。
- Pin、删除及部分直接层级迁移。
- 每日总结、手动总结、周期归档和手动归档。
- AI 请求排队、配置 fallback、HTTP 调用和业务结果回写。

当前核心组件：

| 组件 | 生命周期 | 当前职责 |
|---|---|---|
| `MemoryManager` | `WorldComponent` | 冷启动延迟、每小时 Pawn 状态常识更新、常识库和 PromptCache 持有 |
| `FourLayerMemoryComp` | 每 Pawn `ThingComp` | 四层列表、Scribe、查询门面、低频维护调度 |
| `MemoryMaintainer` | 每 Comp 一个 POCO | 衰减、ABM 到期转换、清理、容量、Pin、删除 |
| `MemorySummarizer` | 每 Comp 一个 POCO | 总结/归档选择、提示词、任务提交和业务回写 |
| `AIService` | 每存档 `GameComponent` | AI 队列、10 秒出队间隔、Payload 检查和回调派发 |
| `ClientPool` / `OpenAIClient` | 跟随 `AIService` | 配置 fallback、网络重试、超时和 OpenAI 兼容协议 |

`IndependentAISummarizer` 和 `AIRequestManager` 已删除，不属于当前维护层。

## 2. 四层状态所有权

`FourLayerMemoryComp` 私有持有四个列表，但通过 public `List<MemoryEntry>` 属性直接暴露：

| 字段 | 属性 | 层级 |
|---|---|---|
| `activeMemories` | `ActiveMemories` | ABM / `Active` |
| `situationalMemories` | `SituationalMemories` | SCM / `Situational` |
| `eventLogMemories` | `EventLogMemories` | ELS / `EventLog` |
| `archiveMemories` | `ArchiveMemories` | CLPA / `Archive` |

四个列表用原有 Scribe key 深存：`activeMemories`、`situationalMemories`、`eventLogMemories`、`archiveMemories`。读档后只做 null 集合恢复，不执行 ID、Layer、排序或跨层重复修复。

当前最终保留 public 可变列表作为数据访问边界。捕获、创建、导入和 Summarizer 可直接 `Add`/`Insert`：

- `JobMemoryCapturer` 和对话捕获 Patch 直接写 ABM。
- 创建记忆窗口按目标层直接 `Insert`。
- 导入功能按目标层直接 `Add`。
- `MemorySummarizer` 成功后直接写 ELS 或 CLPA。
- UI 删除已改走 `MemoryMaintainer.Remove`，Pin 走 `PinMemory`。

## 3. 当前调度

### 3.1 Pawn 级维护

`FourLayerMemoryComp.CompTickRare()` 是个人记忆维护的唯一自动调度入口：

```text
每小时：parent.IsHashIntervalTick(GenDate.TicksPerHour)
  1. MemorySummarizer.DailySummarize()
  2. MemoryMaintainer.RunDecay()
  3. MemoryMaintainer.ConvertActiveMemories()
  4. MemoryMaintainer.CleanupLowActivityMemories()

每天：parent.IsHashIntervalTick(GenDate.TicksPerDay)
  1. MemorySummarizer.PeriodicArchive()
  2. MemoryMaintainer.EnforceMemoryLimits()
```

执行顺序有实际影响：总结/归档先选源并标记 `IsSummarizing`，随后同一次维护中的到期、清理或容量操作仍可能删除这些源条目，因为 Maintainer 当前不检查 `IsSummarizing`。

### 3.2 World 级管理

`MemoryManager.WorldComponentTick()` 不再调度个人记忆衰减、总结或归档。它当前只做：

- 进入会话后等待 200 ticks。
- 每 2500 ticks 运行一次常识维护。
- 开启 `enablePawnStatusKnowledge` 时更新所有殖民者状态常识。
- 清理 Pawn 状态常识更新记录。
- 持久化 `CommonKnowledgeLibrary` 和 `PromptCache`。

`UpdateEventKnowledgeTimePrefixes()` 和 `IsColonyAnimalWithVocalLink()` 仍留在类内，但当前无调用。

## 4. 机械维护

### 4.1 Activity 衰减

`MemoryMaintainer.RunDecay()`：

- ABM 不衰减。
- SCM 使用 `ScmDecayRate`，默认 `0.05`。
- ELS 使用 `ElsDecayRate`，默认 `0.00384`。
- CLPA 使用 `ClpaDecayRate`，默认 `0.00128`。
- `MemoryEntry.Decay()` 对 Pin 条目直接返回。

计算为 `Activity *= 1 - rate`，不做 0-1 clamp。

### 4.2 ABM 到期

`ConvertActiveMemories()` 依赖 ABM 按 `GameTick` 升序排列，并从表头扫描到第一条未到期记忆：

- `AbmLifespanHours <= 0` 时禁用。
- 到期条件是 `currentTick - GameTick > lifespanTicks`。
- `MemoryType.Action` 到期后丢弃。
- 其他类型迁移到 SCM，并把 `Layer` 改为 `Situational`。
- `RoundMemory` 先 `Clone()` 成普通 `MemoryEntry` 再迁移。
- 扫描前缀最终通过 `ABMList.RemoveRange(0, removeCount)` 一次删除。

代码注释仍写“Conversation 转 SCM，其余移除”，但真实判断仅排除 `Action`。因此 Event、Emotion、Relationship、Internal、Summarization 等非 Action 类型若出现在 ABM，也会迁入 SCM。

### 4.3 低活跃清理

`CleanupLowActivityMemories()` 不处理 ABM。SCM、ELS、CLPA 中满足以下条件的条目被删除：

```text
条目为 null
或 Activity < 0.025 且未 Pin
```

`IsUserEdited`、`IsSummarized` 和 `IsSummarizing` 不提供清理保护。

### 4.4 容量治理

每天对 SCM、ELS、CLPA 调用 `TrimToCapacity`：

1. 先删除 null。
2. 仅收集非 Pin 条目。
3. 计算 `nonPinned.Count - max`。
4. 按 Activity 升序、GameTick 升序删除超额条目。

因此当前 `max` 实际限制的是“非 Pin 条目数量”，不是列表总数。Pin 条目可使总列表长度超过配置上限。ABM 不做容量治理。

### 4.5 Pin 与删除

`PinMemory(memory, true)`：

- 非 ABM 只修改 `IsPinned`。
- ABM 先从原列表移除，再改层为 SCM 并加入 SCM。
- ABM 是 `RoundMemory` 时先 Clone，再对克隆设置层级和 Pin。

`Remove(memory)` 会依次从四层删除同一对象引用；位运算 OR 保证四层都执行，可顺便清除同一引用的跨层重复。

## 5. 总结流程

### 5.1 每日触发

`DailySummarize()` 需要：

- `EnableDailySummarization == true`。
- Comp parent 是 `IsColonist == true` 的 Pawn。
- 距 `LastSummarizeTick` 至少一天。
- 按 Pawn 所在经度和 `summarizationHour` 计算出的最近理论总结 tick 大于缓存值。

`_lastSummarizeTick` 是非持久字段。首次读取时根据当前本地时间计算最近理论 tick。一次 `AutoSummarize` 尝试后即更新，即使没有条目、未入队或 AI 最终失败，当天也不会由定时入口重复尝试。

### 5.2 自动选择和分组

`AutoSummarize()` 从 ABM + SCM 选择：

```text
IsSummarizing == false
IsSummarized == false
GameTick < latestPotentialSummarizeTick
```

按 `(latestPotentialSummarizeTick - GameTick) / TicksPerDay` 分组，并按 key 降序提交。key 越大代表周期越早，因此最旧的完整周期先提交。

### 5.3 手动总结

UI 把选中对象传给 `ManualSummarize`。Summarizer 再从当前 ABM + SCM 中按对象引用求交集：

- 只排除 `IsSummarizing == true`。
- 已 `IsSummarized` 的条目允许再次总结。
- 不在 ABM/SCM 的传入对象不会参与。

`SummarizeAll()` 当前只把 `SituationalMemories.Count > 0` 的自由殖民者加入目标列表，然后调用 `AutoSummarize()`。因此“仅有可总结 ABM、SCM 为空”的 Pawn 会被遗漏。

### 5.4 提示词和目标快照

`SummarizeInternal()`：

1. 按 `GameTick` 升序生成源列表。
2. 用 `SummarizePrompt` 和 Pawn `LabelShort` 构建提示词。
3. 每条内容格式为 `AgeString, 12 小时制时间` 加正文。
4. 模板为空或格式错误时回退 `DefaultSummarizePrompt`。
5. 提前创建未入列的 ELS `MemoryType.Summarization` 条目。
6. 目标 Importance 取源平均值，GameTick 取最新源条目，tags/keywords 去重合并。
7. 将源条目 `IsSummarizing = true` 后调用 `AIService.EnqueueAIRequest`。

AI 成功 callback：

- 拒绝空白结果。
- 设置目标 Content 并直接 `ELSList.Add`。
- 把闭包中的源对象标为 `IsSummarized = true`。

dispose 无条件把源对象 `IsSummarizing = false`。AI 失败不创建目标条目，也不设置 `IsSummarized`。

## 6. 归档流程

### 6.1 周期触发

`PeriodicArchive()` 由每日 Comp tick 调用，并再次要求：

- `EnableAutoArchive == true`。
- parent 是殖民者。
- `Parent.IsHashIntervalTick(TicksPerDay * ArchiveIntervalDays)` 命中。

自动归档选择 ELS 中全部 `IsSummarizing == false && IsSummarized == false` 的条目，不按比例截取。

### 6.2 手动归档

显式传入 source 时，只选择：

```text
Type == MemoryType.Summarization
IsSummarizing == false
```

它不强制源对象仍在 ELS，也不排除已经归档过的条目。

### 6.3 结果

归档使用 `ArchivePrompt`，记忆块不附加小时。目标为 CLPA `MemoryType.Summarization`，元数据合并规则与每日总结相同。

AI 成功后直接加入 CLPA，并把源对象 `IsSummarized = true`。源 ELS 不删除，后续可能被衰减、低活跃清理或容量裁剪。AI 失败不创建 CLPA。

## 7. AI 执行和回写

```text
MemorySummarizer
  -> AIService.EnqueueAIRequest
  -> AIService.GameComponentUpdate
  -> 每隔至少 10 秒启动一个任务
  -> ClientPool 按配置 fallback
  -> OpenAIClient 单配置最多 3 次尝试
  -> 有效 Payload 执行业务 callback
  -> finally 执行 dispose
```

重要语义：

- 10 秒是现实时间的任务启动间隔，不是严格单并发。
- AIService 无缓存、无去重、无请求持久化和无取消令牌。
- OpenAIClient 在网络轮询中发现 `Current.Game == null` 会 Abort。
- callback 通过闭包捕获 `memoryList` 和目标 `MemoryEntry`，没有 ID/版本验证。
- 状态写回通常依赖 Unity await 续体回主线程；代码没有显式主线程调度器。
- AI 层详细配置、重试和 fallback 见 `AI层重写方案.md`。

## 8. 关键数据字段

| 字段 | 持久化 | 当前维护语义 |
|---|---|---|
| `Activity` | 是 | 衰减、清理、容量排序 |
| `IsPinned` | 是 | 跳过衰减、低活跃清理和容量淘汰 |
| `IsUserEdited` | 是 | 当前机械维护不提供额外保护 |
| `IsSummarized` | 是 | 自动总结/归档排除；旧存档缺字段时默认 `true` |
| `IsSummarizing` | 否 | 防止 Summarizer 重复选中；Maintainer 不保护 |
| `GameTick` | 是 | ABM 到期、分组、排序和目标时间戳 |
| `Layer` | 是 | 层级标识，但当前可与实际列表不一致 |
| `Id` | 是 | 条目标识，但当前没有统一唯一性校验 |

`MemoryType.Summarization` 是总结和归档产物类型。`Interaction` 仅保留枚举兼容；具体定义见 `MemoryCategory.cs`。

## 9. 配置现状

机械维护：

- `ScmDecayRate = 0.05`
- `ElsDecayRate = 0.00384`
- `ClpaDecayRate = 0.00128`
- `AbmLifespanHours = 24`
- `maxSituationalMemories = 20`
- `maxEventLogMemories = 50`
- `maxArchiveMemories = 50`

总结与归档：

- `EnableDailySummarization = true`
- `summarizationHour = 0`
- `EnableAutoArchive = true`
- `ArchiveIntervalDays = 15`
- `SummarizePrompt` / `ArchivePrompt`
- `SummaryMaxTokens = 8000`

兼容或未被当前维护路径消费：

- `maxActiveMemories`：保留旧 Scribe key，ABM 不限容量。
- `useAISummarization`：仍持久化，但 `MemorySummarizer` 当前不读取，关闭它不会阻止 AI 总结。
- `maxSummaryLength`：仍持久化，但当前提示词和响应处理不读取。

AI Provider 配置见 `AI层重写方案.md`。

## 10. 实现特征

1. 四层属性暴露可变 `List`，捕获、创建、导入和总结结果按各自入口写入。
2. 当前不执行统一 ID 唯一、Layer 一致性或跨层重复检查。
3. Summarizer 成功后直接写入 ELS/CLPA，不经过 Maintainer。
4. `IsSummarizing` 不能防止 Maintainer 删除在途源条目。
5. callback 没有 Pawn、存档会话、源条目存在性或请求版本校验。
6. `AIService.EnqueueAIRequest` 无实例时静默丢弃，无法通知 Summarizer 恢复状态。
7. 仅支持殖民者自动总结/归档，声带链接动物逻辑未启用。
8. `_lastSummarizeTick` 不持久化，记录的是“已尝试”而非“已成功”。
9. 自动总结按最旧完整周期优先提交。
10. `SummarizeAll` 的预筛选忽略仅有 ABM 的 Pawn。
11. ABM 到期算法依赖列表按时间升序；直接 Insert/导入可能破坏此前提。
12. 容量上限只约束非 Pin 数量，Pin 会使总数超过上限。

## 11. 当前结构一句话

> `FourLayerMemoryComp.CompTickRare()` 按 Pawn 哈希分散触发维护，`MemoryMaintainer` 执行机械规则，`MemorySummarizer` 选择源条目并通过 `AIService -> ClientPool -> OpenAIClient` 异步生成 ELS/CLPA，`MemoryManager` 只保留常识与缓存管理；这就是重构结束后的最终维护层结构。
